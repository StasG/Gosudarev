<Page x:Class="Курсовая.Submit"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
      xmlns:local="clr-namespace:Курсовая"
      mc:Ignorable="d" 
      d:DesignHeight="646" d:DesignWidth="500"
      Title="Submit">
    <Grid Height="646" Width="500">
        <StackPanel Orientation="Vertical">
            <Button Command="{Binding Source={StaticResource MainWindowVM}, Path=Print}">Вывести в файл</Button>
        </StackPanel>
        <ScrollViewer Margin="0,24,0,0">
            <StackPanel Name="PodpiskaStack" Orientation="Vertical">
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
